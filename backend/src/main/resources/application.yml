spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/leadzen
    username: nathan
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          model: gemini-3.5-flash